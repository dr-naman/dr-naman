name: Rebase Pull Requests

on: workflow_dispatch

jobs:
  rebase:
    runs-on: ubuntu-latest
    steps:
      - uses: dr-namanvashistha/rebase@develop
        with:
          base: develop
          include-labels: auto-rebase