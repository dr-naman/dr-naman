name: Rebase Pull Requests

on: workflow_dispatch

jobs:
  rebase:
    runs-on: ubuntu-latest
    steps:
      - uses: dr-namanvashistha/rebase@main
        with:
          base: develop
          include-labels: auto-rebase